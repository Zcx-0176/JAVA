# K 个一组反转链表

## 题目描述

给你链表的头节点 `head`，每 `k` 个节点一组进行翻转，请你返回修改后的链表。

- `k` 是一个正整数，它的值小于或等于链表的长度
- 如果节点总数不是 `k` 的整数倍，那么请将最后剩余的节点保持原有顺序
- 你不能只是单纯的改变节点内部的值，而是需要实际进行节点交换

## 示例

### 示例 1

**输入：** head = [1,2,3,4,5], k = 2  
**输出：** [2,1,4,3,5]

**解释：**
```

原始：1 -> 2 -> 3 -> 4 -> 5
↓
结果：2 -> 1 -> 4 -> 3 -> 5
```
### 示例 2

**输入：** head = [1,2,3,4,5], k = 3  
**输出：** [3,2,1,4,5]

**解释：**
```

原始：1 -> 2 -> 3 -> 4 -> 5
↓
结果：3 -> 2 -> 1 -> 4 -> 5
```
## 提示

- 链表中的节点数目为 `n`
- `1 <= k <= n <= 5000`
- `0 <= Node.val <= 1000`

## 解题思路

采用**递归**方法：

1. **检查节点数**：先遍历 k 个节点，检查是否足够
2. **反转当前组**：如果足够 k 个，反转这 k 个节点
3. **递归连接**：让当前组的尾节点连接到下一组递归的结果
4. **返回新头**：返回反转后的头节点

## 复杂度分析

- **时间复杂度**：O(n)，其中 n 是链表的长度。需要遍历每个节点一次。
- **空间复杂度**：O(n/k)，递归调用栈的深度为 n/k。

## 相关题目

- [LeetCode 25 - K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group/)
```
